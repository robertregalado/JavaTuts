public class Foo {
    // notice the naming convention --static final variables
    // are constants, so the name should be all uppercase, with an underscore
    // separating the words
    public static final int FOO_X = 25;
}